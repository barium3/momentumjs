var x = 0;var speed = 5;function setup() {  createCanvas(800, 600);}function draw() {  background(220);    // 画一条中心参考线  stroke(150);  line(400, 0, 400, height);  // 逻辑测试：判断坐标 x  if (x < 400) {    fill(50, 100, 255); // 蓝色    ellipse(x, 300, 80, 80);   } else {    fill(255, 100, 50); // 红色    rect(x, 300, 80, 80);  }  x += speed;  if (x > width || x < 0) {    speed *= -1; // 撞墙反弹，让你观察往返时的逻辑切换  }}