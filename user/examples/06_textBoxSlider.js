// createSlider(min, max, value) creates a slider controller in After Effects.// Read its value with slider.value().// You can keyframe the slider controller in AE to animate the text box size over time.let tWidth;let tHeight;function setup() {  createCanvas(300, 300);  tWidth = createSlider(0, 300, 150);  tHeight = createSlider(0, 300, 180);}function draw() {  background(0);  let tw = tWidth.value();  let th = tHeight.value();  fill(150);  textSize(28);  rectMode(CENTER);  text('The quick brown fox jumps over the lazy dog.', width / 2, height / 2, tw, th);}