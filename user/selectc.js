let mode;function setup() {  createCanvas(240, 200);  mode = createSelect();  mode.option("Circle");  mode.option("Square");  mode.option("Triangle");  mode.selected("Square");}function draw() {  background(245);  fill(30);  noStroke();  if (mode.value() === "Circle") {    circle(width / 2, height / 2, 100);  } else if (mode.value() === "Square") {    rectMode(CENTER);    rect(width / 2, height / 2, 100, 100);  } else {    triangle(      width / 2, height / 2 - 60,      width / 2 - 60, height / 2 + 50,      width / 2 + 60, height / 2 + 50    );  }}