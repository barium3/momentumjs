var numCircle = 10;var circles = [];var clearBGCheckbox;function setup() {  createCanvas(200, 150);    clearBGCheckbox = createCheckbox('meta', false);    frameRate(30);  for (var i = 0; i < numCircle; i++) {    var x = random(width);    var y = random(height);    var r = random(20, 60);    var xspeed = random(-0.25, 0.25);    var yspeed = random(-0.25, 0.25);    circles[i] = new Circle(x, y, r, xspeed, yspeed, i);  }  background(255);}function draw() {  if (clearBGCheckbox.checked()) {    background(255);        for (var i = 0; i < circles.length; i++) {      circles[i].display();     }  }  // 计算相交线  for (var i = 0; i < circles.length; i++) {    circles[i].update();  }    // 移动位置  for (var j = 0; j < circles.length; j++) {    circles[j].move();  }}function Circle(px, py, pr, psp, pysp, pid) {  this.x = px;  this.y = py;  this.r = pr;  this.r2 = this.r * this.r;  this.sp = psp;  this.ysp = pysp;  this.id = pid;  // 新增：绘制圆的方法  this.display = function() {    noFill();    stroke(200, 100); // 浅灰色半透明轮廓    strokeWeight(1);    ellipse(this.x, this.y, this.r * 2);  }  this.update = function() {    for (var i = this.id + 1; i < numCircle; i++) {      intersect(circles[this.id], circles[i]);    }  }  this.move = function() {    this.x += this.sp;    this.y += this.ysp;        // 边界环绕逻辑    if (this.x > width + this.r) this.x = -this.r;    if (this.x < -this.r) this.x = width + this.r;    if (this.y > height + this.r) this.y = -this.r;    if (this.y < -this.r) this.y = height + this.r;  }}function intersect(cA, cB) {  var dx = cA.x - cB.x;  var dy = cA.y - cB.y;  var d2 = dx * dx + dy * dy;  var d = sqrt(d2);  if (d === 0 || d > cA.r + cB.r || d < abs(cA.r - cB.r)) return;  var a = (cA.r2 - cB.r2 + d2) / (2 * d);  var h = sqrt(max(0, cA.r2 - a * a));  var x2 = cA.x + a * (cB.x - cA.x) / d;  var y2 = cA.y + a * (cB.y - cA.y) / d;  var paX = x2 + h * (cB.y - cA.y) / d;  var paY = y2 - h * (cB.x - cA.x) / d;  var pbX = x2 - h * (cB.y - cA.y) / d;  var pbY = y2 + h * (cB.x - cA.x) / d;  // 设置线条样式  strokeWeight(1);  if (clearBGCheckbox.checked()) {    stroke(50, 180); // 骨架模式下线条深一些  } else {    stroke(dist(paX, paY, pbX, pbY) * 4, 12); // 留痕模式保持原有逻辑  }    line(paX, paY, pbX, pbY);}