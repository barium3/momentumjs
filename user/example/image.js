let img;// Load the image.function preload() {  img = loadImage('apple.png');}function setup() {  createCanvas(100, 100);  background(50);  // Resize the image.  // Display the resized image.  image(img, 0, 0,100,100);    // Get a pixel's color.  let c = img.get(600, 300);  // Style the square using the pixel's color.  fill(c);  noStroke();  // Draw the square.  square(25, 25, 50);}