function setup() {  createCanvas(800, 600);}function draw() {  background(240);    var val = (frameCount % 100);  // 深度嵌套测试  if (val > 20) {    if (val < 80) {      if (val > 40 && val < 60) {        fill(0, 255, 0); // 中间态        rect(300, 200, 200, 200);      } else {        // 测试并列的 else 是否会被上一层 if 误拦截        if (val <= 40) {          fill(0, 0, 255);          ellipse(400, 300, 100, 100);        } else {          fill(255, 255, 0);          triangle(400, 200, 350, 300, 450, 300);        }      }    } else {      // val >= 80      background(0);     }  } else {    // val <= 20    fill(255, 0, 0);    rect(10, 10, 50, 50);  }}