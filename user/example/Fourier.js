var time = 0;var wave = []; // 存储最终波形的路径function setup() {  createCanvas(800, 400);}function draw() {  background(30);  translate(150, 200);  var x = 0;  var y = 0;  // 模拟傅里叶级数（Fourier Series）：叠加 5 个正弦波  // 逻辑：每一层循环代表一个频率分量  for (var i = 0; i < 5; i++) {    var prevX = x;    var prevY = y;    // 傅里叶公式简化版：频率越高，半径越小    var n = i * 2 + 1; // 奇数序列: 1, 3, 5, 7, 9    var radius = 75 * (4 / (n * PI)); // 振幅逻辑        // 计算当前圆周运动的坐标    x += radius * cos(n * time);    y += radius * sin(n * time);    // 绘制逻辑判断：只在第一层和最后一层画辅助圆    if (i === 0 || i === 4) {      stroke(255, 100);      noFill();      ellipse(prevX, prevY, radius * 2);      stroke(255);      line(prevX, prevY, x, y);    }  }  // 将计算出的 y 坐标存入数组头部  wave.unshift(y);  // 逻辑：将圆周运动的 y 映射到右侧的波形图中  translate(200, 0);  line(x - 200, y, 0, wave[0]);    beginShape();  noFill();  stroke(0, 255, 255);  for (var j = 0; j < wave.length; j++) {    vertex(j, wave[j]);  }  endShape();  time += 0.05;  // 性能优化逻辑：防止数组无限增大  if (wave.length > 400) {    wave.pop();  }}