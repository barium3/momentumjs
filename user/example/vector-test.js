// p5.Vector 向量示例 - 简单版function setup() {  createCanvas(800, 600);  frameRate(30);}function draw() {    background(200,50);  // 创建向量  var center = createVector(width / 2, height / 2);  // 使用时间创建动态效果  var angle = frameCount * 0.05;  // 绘制多个围绕中心旋转的圆  for (var i = 0; i < 8; i++) {    // 从角度创建方向向量    var dir = p5.Vector.fromAngle(angle + (i * PI) / 4);    // 设置向量长度    dir.setMag(150);    // 计算位置    var pos = p5.Vector.add(center, dir);    // 设置颜色    fill(i * 30, 200, 255);    noStroke();    // 绘制圆    circle(pos.x, pos.y, 40);  }}