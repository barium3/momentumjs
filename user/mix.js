let sizeCtrl;let angleCtrl;let colorCtrl;let showDotCtrl;let shapeCtrl;let anchorCtrl;function setup() {  createCanvas(520, 320);  sizeCtrl = createSlider(20, 180, 90, 1);  angleCtrl = createAngle(45);  colorCtrl = createColorPicker(255, 120, 40);  showDotCtrl = createCheckbox("Show Dot", true);  shapeCtrl = createSelect();  shapeCtrl.option("circle");  shapeCtrl.option("square");  shapeCtrl.option("triangle");  shapeCtrl.selected("circle");  anchorCtrl = createPoint(width * 0.5, height * 0.58);  rectMode(CENTER);  textAlign(CENTER, CENTER);}function draw() {  background(18);  let s = sizeCtrl.value();  let deg = angleCtrl.value();  let c = colorCtrl.color();  let shape = shapeCtrl.selected();  let showDot = showDotCtrl.checked();  let x = anchorCtrl.x();  let y = anchorCtrl.y();  push();  translate(x, y);  rotate(radians(deg));  noStroke();  fill(c);  if (shape === "circle") {    ellipse(0, 0, s, s);  } else if (shape === "square") {    rect(0, 0, s, s);  } else if (shape === "triangle") {    triangle(      0, -s * 0.55,      -s * 0.5, s * 0.35,      s * 0.5, s * 0.35    );  }  if (showDot) {    fill(255);    ellipse(0, 0, 8, 8);  }  pop();  noFill();  stroke(90);  ellipse(x, y, s + 24, s + 24);  noStroke();  fill(200);  text("size: " + nf(s, 1, 0), width * 0.5, 24);  text("angle: " + nf(deg, 1, 1), width * 0.5, 44);  text("shape: " + shape, width * 0.5, 64);}