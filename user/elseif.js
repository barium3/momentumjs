function setup() {  createCanvas(800, 600);}function draw() {  background(0);  noStroke();  var cols = 4;  var rows = 3;  var spacing = 200;  for (var i = 0; i < cols; i++) {    for (var j = 0; j < rows; j++) {            // 核心逻辑：      // 利用 i+j 的和与 frameCount 进行偏移比较      // 产生一种“逻辑波浪”穿过屏幕的效果      var wave = (i + j + frameCount * 0.2) % 10;      if (wave < 3) {        // 逻辑 A：波峰状态        fill(255, 0, 100);        ellipse(i * spacing + 20, j * spacing + 20, 30, 30);      } else if (wave < 6) {        // 逻辑 B：过渡状态        fill(0, 200, 255);        rectMode(CENTER);        rect(i * spacing + 20, j * spacing + 20, 15, 15);      } else {        // 逻辑 C：背景/静默状态        fill(50);        ellipse(i * spacing + 20, j * spacing + 20, 5, 5);      }    }  }}